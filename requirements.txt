# 飞书 AI 新闻推送 Bot 依赖
# Python 3.10+
#
# 注意：此为最低版本约束（>=），精确锁定版本见 requirements-lock.txt
# 生产部署请使用: pip install -r requirements-lock.txt

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# LangGraph 工作流
langgraph>=0.2.0
langchain-core>=0.3.0

# LLM 多厂商支持
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0

# 配置管理
pydantic-settings>=2.5.0

# 数据库
sqlalchemy>=2.0.0
pymysql>=1.1.0
redis>=5.0.0

# 定时任务
apscheduler>=3.10.0

# 数据抓取
feedparser>=6.0.0
trafilatura>=1.12.0

# 重试与容错
tenacity>=8.0.0

# Prompt 管理
pyyaml>=6.0

# HTTP 请求
httpx>=0.27.0

# 飞书官方 SDK（长连接模式 + API 调用）
lark-oapi==1.5.5

# Telegram Bot SDK
python-telegram-bot>=21.0

# Discord Bot SDK（网关模式，无需公网 URL）
discord.py>=2.3.0

# 可观测性
prometheus_client>=0.21.0
python-json-logger>=2.0.0

# RAG 向量检索
chromadb>=0.5.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0
